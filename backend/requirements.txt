# Pizza Denfert Loyalty — backend dependencies (clean, minimal).
#
# Only the packages the backend actually imports (see server.py). The previous
# file was a full `pip freeze` of a shared venv and pulled in unused Google AI /
# protobuf / litellm packages that caused the protobuf ResolutionImpossible.
# None of those are used here, so they are intentionally gone.

# Web framework + server
fastapi==0.110.1
starlette==0.37.2
uvicorn[standard]==0.25.0

# Config
python-dotenv==1.2.2

# MongoDB
motor==3.3.1
pymongo==4.6.3

# Models / validation (EmailStr needs email-validator)
pydantic==2.13.4
email-validator==2.3.0

# Auth / crypto
bcrypt==4.1.3
PyJWT==2.13.0

# HTTP client (SMS providers, Emergent push relay)
httpx==0.28.1

# Web push (VAPID)
pywebpush==2.0.0
